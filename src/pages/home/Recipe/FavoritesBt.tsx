import { IconButton } from "@mui/material";
import FavoriteBorder from "../../../icons/FavoriteBorder";

const FavoritesBt: React.FC = () => (
  <IconButton>
    <FavoriteBorder sx={{ fontSize: 46 }} />
  </IconButton>
);

export default FavoritesBt;
